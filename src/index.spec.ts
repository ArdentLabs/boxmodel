import { equal } from 'assert'

describe('generate function', () => {
  it('should work', () => {
    const expected: string = 'bye'
    const actual: string = 'hi'
    equal(actual, expected)
  })
})
